name: ${project.name}
version: ${project.version}
description: ${project.description}
website: ${project.url}
authors: [ELCHILEN0]
main: com.TeamNovus.Supernaturals.Supernaturals
commands:
  supernaturals:
    aliases: [supernatural, sn]
soft-depend: [ProtocolLib, TagHelper]